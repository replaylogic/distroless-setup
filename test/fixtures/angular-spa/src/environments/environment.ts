export const environment = {
  production: false,
  apiBaseUrl: 'https://api.dev.example.com',
  featureFlag: false,
  retries: 3,
  auth: {
    clientId: 'dev-client',
    scope: 'openid profile',
  },
};
